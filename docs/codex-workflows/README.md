# Codex Workflows Working Artifacts

This directory is the default home for new Codex Workflows design specs and implementation plans when a target repository does not define a more specific location.

Historical pre-rebrand plans and specs live under `docs/archive/superpowers/` and are not live package authority.
